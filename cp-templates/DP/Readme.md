# Dynamic Programming (DP)
## 🧠 Что такое Dynamic Programming
Dynamic Programming (DP) — это метод решения задач, при котором мы разбиваем задачу на перекрывающиеся подзадачи и запоминаем их решения, чтобы не пересчитывать одно и то же много раз.
#### DP особенно эффективен, когда:
- задача имеет перекрывающиеся подзадачи;
- и соблюдается оптимальная подструктура — решение задачи можно составить из решений её подзадач.
## 🔍 Пример
Задача: найти n-е число Фибоначчи

#### Без DP (рекурсия):

```int fib(int n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2); // повторно считаем одно и то же
}
```

#### С DP (мемоизация):

```int fib(int n, vector<int>& dp) {
    if (n <= 1) return n;
    if (dp[n] != -1) return dp[n]; // используем ранее посчитанное
    return dp[n] = fib(n - 1, dp) + fib(n - 2, dp);
}
```

#### Итеративная (табуляция):

```int fib(int n) {
    vector<int> dp(n + 1);
    dp[0] = 0; dp[1] = 1;
    for (int i = 2; i <= n; i++)
        dp[i] = dp[i - 1] + dp[i - 2];
    return dp[n];
}
```
## 🧩 Основные виды DP

| Вид | Описание | Пример |
|-----|-----------|--------|
| **Top-down (мемоизация)** | Рекурсия с запоминанием результатов | `fib(n)` с `dp` |
| **Bottom-up (табуляция)** | Итеративное заполнение таблицы | массив `dp[]` |
| **Space optimization** | Оптимизация памяти, если хранятся только последние значения | Фибоначчи с двумя переменными |
| **1D DP** | Решения зависят от одной переменной (`n`) | `fib(n)` |
| **2D DP** | Используется матрица (`dp[i][j]`) | рюкзак, редакт. расстояние |
| **Bitmask DP / Tree DP** | Более продвинутые типы | задачи на подмножества, графы |

## 🧠 Как решать задачу DP

1. Определи состояние — что означает `dp[i]` или `dp[i][j]`.  
2. Сформулируй рекуррентное соотношение — из каких подзадач зависит текущее состояние.  
3. Определи базовые случаи — `dp[0]`, `dp[1]` и т. д.  
4. Выбери способ — **Top-Down** (рекурсия с мемоизацией) или **Bottom-Up** (табуляция).  
5. Проверь оптимизацию по памяти — можно ли хранить меньше данных.  

## ⚔️ Полезные советы

- Если рекурсия начинает повторять одинаковые вызовы → попробуй DP.
- Рисуй таблицу dp и смотри, как она заполняется.
- Оптимизация памяти возможна, если текущий слой зависит только от предыдущего.
- Иногда DP можно заменить жадным алгоритмом, если структура задачи позволяет.

## 🔗 Полезный ресурс

📘 **[Введение в динамическое программирование — CP Algorithms](https://cp-algorithms.com/dynamic_programming/intro-to-dp.html)**  
Отличная статья, объясняющая основы динамического программирования:  
- что такое подзадачи и перекрытия подзадач  
- как строить рекуррентные соотношения  
- примеры топ-даун и боттом-ап подходов

## Пример задачи **70. Climbing Stairs** 

📄 [Решение →](./Examples.cpp)














